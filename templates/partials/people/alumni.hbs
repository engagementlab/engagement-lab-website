{{!< default}}
{{!-- 
/**
 * Engagement Lab Website
 * Developed by Engagement Lab, 2015
 * ==============
 * 
 * Team member partial 
 * - See Team model for fields
 * - Use {{jsonPrint project}} to print entire json...DEBUG ONLY
 * ==========
 */
--}}
<div class="col-sm-6 col-md-4 team-member alumni-item {{person.cohortYear.key}} {{#each person.keywords}}{{this.key}} {{/each}}">
	
	<a href="/people/{{person.key}}">
	<div class="image-box style-2 mb-20 text-center">
			
			<img id="{{cleanString person.name.first}}-{{cleanString person.name.last}}" src="{{{cloudinaryUrl person.image width=192 height=192}}}" alt="{{person.name.first}} {{person.name.last}}" class="photo">
			
			<div class="body">
				<h3 class="theme-primary">{{person.name.first}} {{person.name.last}}</h3>

					<p>{{person.cohort}}</p>

			</div>

	</div>
	</a>
	{{#if person.project}}
	<div class="cmap-proj">{{{person.project.html}}}</div>
	{{/if}}
</div>