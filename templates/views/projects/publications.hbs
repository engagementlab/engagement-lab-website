{{!< default}}
{{!--
/**
* Engagement Lab Website
* Developed by Engagement Lab, 2015
* ==============
*
* Publications Directory Page Template
* - See Publications model for fields
* - Use {{jsonPrint project}} to print entire json...DEBUG ONLY
* ==========
*/
--}}
<!-- PUBLICATIONS START -->
<section class="main-container">
	<div class="container">
		<div class="main">
			<div class="row">
				<div class="col-md-12">
					<h1 class="page-title"><strong>Publications</strong></h1>
					{{> page/lead}}
				</div>
			</div>
		</div>
	</div>

	{{!-- <div class="row filters">
		<div class="btn-group clearfix">
			<button id="author-{{key}}" class="syllabi-options btn btn-default dropdown-toggle" type="button" data-filter-group="{{label}}" data-toggle="dropdown">
			<div class="button-text">{{label}} <span class="{{key}}-number filter-counter"> </span><span class="bigCaret"></span></div>
			</button>
			<ul class="dropdown-menu scrollable" role="menu">
				<li><a id="authors-all" data-filter=""  href="#" data-filter-value="" data-filter-group="{{key}}" class="all-options option-authors">All Authors</a></li>
				{{#each authors}}
				<li><a id="{{../key}}" value="#" href="#" data-filter-value=".{{key}}" data-filter-group="{{../key}}" class="option-{{../key}}">{{name}}</a></li>
				{{/each}}
			</ul>
		</div>
		{{/each}}
		<div class="btn-group clearfix">
			<button class="syllabi-options clear-all btn btn-default dropdown-toggle" type="button" data-filter-value="">Reset</button>
		</div>
	</div> --}}

	{{#if publications}}
		<div class="container">
			{{#each publications}}
				<a id="{{trimPluralize @key}}" name="{{trimPluralize @key}}">&nbsp;</a>
				<div class="row">
					<div class="col-md-12">
						<h3>{{pluralize @key}}</h3>
						<div class="separator-5"></div>
					</div>
				</div>
				{{!-- filters start --}}
					{{!-- 				
						{{#if this.sub_sections}}
							<div class="row">
								<div class="col-md-12">
									<div class="filters mb-40">
										<ul class="nav nav-pills">
											<li class="active"><a href="#" data-filter=".{{trim @key}}, .none">All</a></li>
											{{#each this.sub_sections}}
											<li><a href="#" data-filter=".{{trim @key}}.{{key}}, .none">{{name}}</a></li>
											{{/each}}
										</ul>
									</div>
								</div>
							</div>
						{{/if}} 
					--}}
				{{!-- filters end --}}
						
				{{#if this.isArticle}}
				<div class="row">
					{{#each records}}
						{{> publications/list_item listing=this}}
					{{/each}}
				</div>
				{{else}}
				<div class="row">
					{{#each records}}
						{{> publications/grid_item listing=this}}
					{{/each}}
				</div>
				{{/if}}

			{{/each}}
		</div>
	{{/if}}
</section>